import { ArtworkColors } from '@/helpers/gradientColorSorter';

export interface Chapter {
  author: string;
  bookTitle: string;
  chapterTitle: string;
  chapterNumber: number;
  chapterDuration: number;
  startMs: number;
  url: string;
  isAutoGenerated?: boolean;
}

export interface Book {
  bookId: string;
  author: string;
  bookTitle: string;
  chapters: Chapter[];
  artwork: string | null;
  artworkHeight: number | null;
  artworkWidth: number | null;
  artworkColors: ArtworkColors | null;
  selectedAccentColorType: string | null;
  bookDuration: number;
  bookProgress: {
    currentChapterIndex: number;
    currentChapterProgress: number | null;
  };
  bookProgressValue: number;
  /** ms since epoch of the last playback start; null if never played since column added */
  lastPlayedAt: number | null;
  /** ms since epoch of when the book was last marked Finished; null unless currently Finished */
  finishedAt: number | null;
  hasAutoGeneratedChapters?: boolean;
  isSingleFile?: boolean;
  metadata: {
    [key: string]: any;
  };
}

export interface Author {
  name: string;
  books: Book[];
}

export type BookEditableFields = Pick<Book, 'bookTitle' | 'author'> & {
  [K in 'narrator' | 'genre' | 'year' | 'description' | 'copyright']?:
    | string
    | null;
};
